package abstraction;// interface  is like a second parent whose methods can be called in other child class by writing the word "implements" 
// this is used to go around multiple inheritance that is not allowed in JAVA

public interface SpecialBehavior {// interface consider all methods as abstract and do not require a body.
	
	public void pet();

}
